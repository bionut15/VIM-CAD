// define global keymaps here with the default files
// also should be referenced everywhere
use crate::app::state::{MainWindow, StateApp, Transition::*};
use bevy::prelude::*;

pub fn keyboard_input(keys: Res<ButtonInput<KeyCode>>) {
    //let mut window = MainWindow::new();
    todo!();
    //if keys.just_pressed(KeyCode::KeyI) {
    //    window.change(InsertT);
    //}
}
